/**
 * A listener interface to handle end of a game.
 */
interface EndgameListener {
    /**
     * In the implementation it will execute the desired action, when a game has ended.
     */
    void someGameEnded();
}
