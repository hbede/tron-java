import javax.swing.*;
import java.awt.*;

/**
 * It is a class that should contain the settings for the game.
 * It is not used yet.
 */
public class SettingsPanel extends JPanel {

    /**
     * Constructor for class SettingsPanel.
     * Allows customizability.
     */
    public SettingsPanel() {
        setBackground(new Color(0, 0, 0));
        setFocusable(true);
    }
}
